services:
  autopwn-api:
    build: .
    ports:
      - "8000:8000"
    command: uvicorn ctf_autopwn.api.server:app --host 0.0.0.0 --port 8000
    environment:
      AUTOPWN_LOG_LEVEL: info
      AUTOPWN_DB: /data/runs.db
    volumes:
      - autopwn-data:/data

volumes:
  autopwn-data:
